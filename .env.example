# Frontend environment — copy this file to ".env" and adjust as needed.

# Base URL of the CIPHER Express API (see server/).
# Defaults to http://localhost:4000 when unset.
VITE_API_URL=http://localhost:4000
